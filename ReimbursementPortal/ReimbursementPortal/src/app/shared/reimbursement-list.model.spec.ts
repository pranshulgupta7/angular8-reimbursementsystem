import { ReimbursementList } from './reimbursement-list.model';

describe('ReimbursementList', () => {
  it('should create an instance', () => {
    expect(new ReimbursementList()).toBeTruthy();
  });
});
